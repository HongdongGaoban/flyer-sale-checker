/**
 * config.js
 * 環境変数から設定を取得する（GAS の Config.gs / PropertiesService 相当）
 *
 * 必須環境変数:
 *   GEMINI_API_KEY   : Google AI Studio で取得した API キー
 *   ESTAT_APP_ID     : e-Stat API アプリケーション ID
 *   GCP_PROJECT_ID   : Firestore を持つ GCP プロジェクト ID
 *
 * オプション環境変数:
 *   PORT             : サーバーポート（デフォルト: 8080）
 *   API_SECRET       : 簡易認証キー（未設定時は認証スキップ）
 */

// ローカル開発時のみ .env を読み込む（本番 Cloud Run では環境変数として注入）
if (process.env.NODE_ENV !== 'production') {
  const { default: dotenv } = await import('dotenv');
  dotenv.config();
}

export function getConfig() {
  return {
    geminiApiKey:  process.env.GEMINI_API_KEY  ?? '',
    eStatAppId:    process.env.ESTAT_APP_ID    ?? '',
    gcpProjectId:  process.env.GCP_PROJECT_ID  ?? '',
    port:          parseInt(process.env.PORT ?? '8080', 10),
    apiSecret:     process.env.API_SECRET      ?? '',
  };
}

// ============================================================
// 判定閾値（Config.gs の JUDGMENT 定数相当）
// ============================================================
export const JUDGMENT = {
  EXCELLENT_THRESHOLD: -0.15,  // -15% 以下 → ◎ お買い得
  GOOD_THRESHOLD:      -0.05,  // -15%〜-5% → ○ やや安め
};

// ============================================================
// 生鮮カテゴリ判定エイリアス（正規名 → 表記ゆれ配列）
// 正規名は getEStatTargets().category / Firestore marketData のドキュメントIDと一致
// ============================================================
export const FRESH_FOOD_ALIASES = {
  // ---- 野菜類 ----
  'キャベツ':     ['キャベツ', 'きゃべつ'],
  'レタス':       ['レタス', 'れたす'],
  'にんじん':     ['にんじん', 'ニンジン', '人参'],
  'たまねぎ':     ['たまねぎ', 'タマネギ', '玉ねぎ', '玉葱', '玉ネギ'],
  'ほうれん草':   ['ほうれん草', 'ほうれんそう', 'ホウレンソウ', 'ホウレン草'],
  'トマト':       ['トマト', 'とまと'],
  'きゅうり':     ['きゅうり', 'キュウリ', '胡瓜'],
  'なす':         ['なす', 'ナス', '茄子', 'なすび', 'ナスビ'],
  'ピーマン':     ['ピーマン', 'ぴーまん'],
  'ねぎ':         ['ねぎ', 'ネギ', '葱', '長ねぎ', '長ネギ'],
  'もやし':       ['もやし', 'モヤシ'],
  'じゃがいも':   ['じゃがいも', 'ジャガイモ', 'じゃが芋', 'ジャガ芋', '馬鈴薯'],
  '大根':         ['大根', 'だいこん', 'ダイコン'],
  // ---- 肉類 ----
  '牛肉':         ['牛肉', '牛ロース', '牛バラ', '牛もも'],
  '豚肉':         ['豚肉', '豚バラ', '豚ロース', '豚もも'],
  '豚肉こま切れ': ['豚肉こま切れ', '豚こま切れ', '豚こま', '豚コマ'],
  '鶏肉':         ['鶏肉', 'とり肉', '鶏もも', '鶏むね', '鶏胸', 'もも肉', 'むね肉', '胸肉'],
  // ---- 卵・牛乳 ----
  '卵':           ['卵', 'たまご', 'タマゴ', '玉子', '鶏卵'],
  '牛乳':         ['牛乳'],
  // ---- 追加野菜（Phase 1） ----
  'はくさい':     ['はくさい', 'ハクサイ', '白菜'],
  'ブロッコリー': ['ブロッコリー', 'ぶろっこりー'],
  'さつまいも':   ['さつまいも', 'サツマイモ', '薩摩芋', 'さつま芋'],
  // ---- 豆腐（Phase 1） ----
  '豆腐':         ['豆腐', 'とうふ', 'トウフ'],
  // ---- 果物（Phase 1） ----
  'バナナ':       ['バナナ', 'ばなな'],
  // ---- 追加野菜（Phase 2） ----
  'かぼちゃ':     ['かぼちゃ', 'カボチャ', '南瓜'],
  '生しいたけ':   ['生しいたけ', 'しいたけ', 'シイタケ', '椎茸', '生椎茸'],
  'えのきたけ':   ['えのきたけ', 'えのき', 'エノキ', 'エノキタケ'],
  'しめじ':       ['しめじ', 'シメジ', 'ぶなしめじ', 'ブナシメジ'],
  // ---- 果物（Phase 2） ----
  'りんご':       ['りんご', 'リンゴ', '林檎', 'ふじ', 'つがる', '王林'],
  'みかん':       ['みかん', 'ミカン', '蜜柑', '温州みかん', '温州ミカン'],
};

// ============================================================
// Gemini API 設定（Config.gs 相当）
// ============================================================
export const GEMINI_MODEL    = 'gemini-2.5-flash';
export const GEMINI_API_BASE = 'https://generativelanguage.googleapis.com/v1beta/models/';

// ============================================================
// e-Stat API 設定（Config.gs 相当）
// ============================================================
export const ESTAT_API_BASE      = 'https://api.e-stat.go.jp/rest/3.0/app/json/getStatsData';
export const ESTAT_STATS_DATA_ID = '0003421913';
export const ESTAT_AREA_CODE     = '04100';  // 仙台市
